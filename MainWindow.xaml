<Window
  x:Class="HKW.WPF.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:local="clr-namespace:HKW.WPF"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:t="clr-namespace:HKW.WPF.Triggers"
  Title="MainWindow"
  Width="800"
  Height="450"
  x:ClassModifier="internal"
  mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" MinWidth="100" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <StackPanel>
      <Button Content="1" />
      <Button Content="2" />
      <Button Content="3" />
      <Button Content="4" />
      <Button Content="5">
        <i:Interaction.Triggers>
          <i:EventTrigger EventName="Click">
            <t:ScrollToControlAction ScrollViewer="{Binding ElementName=ScrollViewer_Main}" TargetControl="{Binding ElementName=Button_4}" />
          </i:EventTrigger>
        </i:Interaction.Triggers>
      </Button>
    </StackPanel>
    <ScrollViewer
      x:Name="ScrollViewer_Main"
      Grid.Column="1"
      VerticalScrollBarVisibility="Auto">
      <StackPanel>
        <Button
          Height="100"
          Margin="10"
          Content="111" />
        <Button
          Height="100"
          Margin="10"
          Content="222" />
        <Button
          Height="100"
          Margin="10"
          Content="333" />
        <Button
          x:Name="Button_4"
          Height="100"
          Margin="10"
          Content="444" />
        <Button
          x:Name="Button_5"
          Height="100"
          Margin="10"
          Content="555" />
      </StackPanel>
    </ScrollViewer>
  </Grid>
</Window>
